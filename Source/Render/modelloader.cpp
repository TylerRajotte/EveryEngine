#include "modelloader.h"
#include "../dependancies.h"

bool ModelLoader::OpenModel(int ID){
    std::ofstream ModelFile;
    std::stringstream IDAsString;

    ModelFile.open(GameObjectsLocation + "")

    return true;
}