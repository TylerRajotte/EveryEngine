// This is a comment in the settings file
// Comments should also be able to work haveway through a line

// Also the spaces around the equals should be ignored
// True and False shouldn't be case sensetive

// Settings should be able to go in any order

// The bellow space should be ingored

WindowName = "EveryEngine"
WindowXSize = 640
WindowYSize = 480
WindowXpos = CENTERED
WindowYpos = CENTERED
// Above is a statment that can be either a number or a constant 

EnableVSync = False
OpenGLShadersLocation = "Shaders/"

SDLOpenGLAttribute = (SDL_GL_DOUBLEBUFFER, 1)
// Above is a reusable setting that can be reused and will add more statements

